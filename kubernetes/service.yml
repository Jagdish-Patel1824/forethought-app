apiVersion: v1
kind: Service
metadata:
  name: node-app-service
  labels:
    app: node-app   

spec: 
  type: NodePort
  selector:
    app: node-app
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30007